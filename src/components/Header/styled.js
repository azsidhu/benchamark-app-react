import styled from 'styled-components'

const HeaderImage = styled.img`
  margin-right: 100px;
  width: 30px;
  height: 30px;
`

const NavListItem = styled.li`
  margin-left: 20px;
  padding-bottom: 10px;
`

const LogoutText = styled.h5`
  color: white;
  cursor: pointer;
`

const LogoutDiv = styled.div`
  float: left;
`

const TopNav = styled.nav`
  padding-bottom: 2px;
`

export { HeaderImage, NavListItem, LogoutText, LogoutDiv, TopNav }
